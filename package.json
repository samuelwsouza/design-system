{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "turbo": "^2.0.12",
    "vite": "^5.4.0"
  },
  "packageManager": "npm@10.5.0",
  "name": "with-vite",
  "engines": {
    "node": ">=20.0.0"
  }
}
